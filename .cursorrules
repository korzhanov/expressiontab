# We are building ExpressionTab — Chrome new-tab speed-dial from history & bookmarks (Svelte + Vite + TypeScript)

When you write code, be 100% sure that it will work and won't break anything that exists.
The fewer lines of code the better.
Act like several cool professionals at once: senior Chrome extension engineer, senior Svelte frontend engineer, performance specialist, UX designer, testing specialist.
Don't stop until you've implemented this feature to completion, with the option to add a completion report.
Before proceeding, give me a brief description of the current state.
Answer briefly.
You should not delete comments; you should add comments everywhere (Russian line comments — see `.cursor/rules/russian-line-comments.mdc`).
Break it down into necessary steps, including only the really necessary steps.
Prefer `bun` over `node`/`npm`/`yarn` for scripts and tests (`bun test`, `bun run build`).
GitHub flow: issue → branch → PR → project #9 (see `.cursor/rules/`).
Public repo security: never commit secrets/PAT; least-privilege permissions; see `.cursor/rules/public-repo-security.mdc`.
Do not mention other private/internal repos by name in docs, issues, PRs, or chat — keep this project self-contained.
